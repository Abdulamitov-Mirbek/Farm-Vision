{
  "name": "agro-suite",
  "version": "1.0.0",
  "description": "Умная платформа для сельского хозяйства",
  "main": "server/server.js",
  "scripts": {
    "install-all": "npm install && cd client && npm install && cd ../server && npm install",
    "dev": "concurrently \"cd server && npm run dev\" \"cd client && npm run dev\"",
    "vercel-build": "npm run install-all && npm run build",
    "start": "cd server && npm start",
    "build": "cd client && npm run build",
    "test": "concurrently \"cd server && npm test\" \"cd client && npm test\""
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  },
  "keywords": [
    "agriculture",
    "farm",
    "ai",
    "react",
    "nodejs"
  ],
  "author": "",
  "license": "MIT",
  "engines": {
    "node": ">=18.0.0"
  },
  "dependencies": {
    "recharts": "^3.7.0"
  }
}
